name: 'SedStart RunCI'
description: 'Trigger test runs on SedStart and stream SSE logs.'
author: 'Your Name'
branding:
  icon: 'play'
  color: 'green'

inputs:
  api_key:
    description: 'SedStart API key (use GitHub Secret)'
    required: true
  project_id:
    description: 'Project ID'
    required: true
  test_id:
    description: 'Test ID'
    required: true
  profile_id:
    description: 'Profile ID'
    required: true
  browser:
    description: 'Browser (chrome|firefox)'
    required: false
    default: 'chrome'
  headless:
    description: 'true or false'
    required: false
    default: 'true'
  environment:
    description: 'QA or Prod (default: Prod)'
    required: false
    default: 'Prod'

runs:
  using: 'node20'
  main: 'dist/index.js'

